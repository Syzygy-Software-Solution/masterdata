_schema-version: 3.3.0
ID: master_data
description: A simple CAP project.
version: 1.0.0
modules:
- name: master_data-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: master_data-auth
  - name: master_data-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: master_data-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: master_data-db
  parameters:
    buildpack: nodejs_buildpack
- name: master_data-destination-content
  type: com.sap.application.content
  requires:
  - name: master_data-destination-service
    parameters:
      content-target: true
  - name: master_data_html_repo_host
    parameters:
      service-key:
        name: master_data_html_repo_host-key
  - name: master_data-auth
    parameters:
      service-key:
        name: master_data-auth-key
  parameters:
    content:
      instance:
        destinations:
        - Name: masterdata_master_data_html_repo_host
          ServiceInstanceName: master_data-html5-app-host-service
          ServiceKeyName: master_data_html_repo_host-key
          sap.cloud.service: masterdata
        - Authentication: OAuth2UserTokenExchange
          Name: masterdata_master_data_auth
          ServiceInstanceName: master_data-auth
          ServiceKeyName: master_data-auth-key
          sap.cloud.service: masterdata
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: master_data-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: master_data_html_repo_host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - masterdata.zip
      name: masterdata
      target-path: resources/
- name: masterdata
  type: html5
  path: app/masterdata
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: master_data-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: master_data-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: master_data-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: master_data-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: master_data-destination-service
    service-plan: lite
  requires:
  - name: srv-api
- name: master_data_html_repo_host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: master_data-html5-app-host-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
